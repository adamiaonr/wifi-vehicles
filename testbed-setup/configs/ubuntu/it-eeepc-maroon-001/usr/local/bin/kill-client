#!/bin/bash

ps -ef | grep run-client | awk '{print $2}' | sudo xargs -r kill -9
pkill -f tcpdump
pkill -f get-cpu
pkill -f get-ntpdate
pkill -f get-cbt
pkill -f get-gps
/usr/local/bin/kill-server "it@10.10.10.113"
pkill -f consumer

exit 0
